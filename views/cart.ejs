<!DOCTYPE html>
<html>

<head>
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" type="image/png" href="./images/flower-icon.png" />
    <link rel="stylesheet" type="text/css" href="./vendor/normalize.css">
    <link rel="stylesheet" type="text/css" href="./styles/main.css">
    <script src="./scripts/main.js"></script>
    <meta http-equiv="content-type" content="text/html; charset=utf-8" />

</head>

<body class="content">

    <%- include(`partials/header`); %>
    
    <main class="main">
        <div class="container2">
            <h1 class="decorated-title decorated-title-centered font-secondary main-color">
                <span class="main-color-emphasized">Leaderboard</span> overview
            </h1>

            <% if (!users) { %>
                <div class="entry3 entry-left gutter">
                    No registered users
                </div>
            <% } else { %>
                <div class="gutter">
                    <div class="entry3 entry-emphasized">
                        <div>#</div>
                        <div>User</div>
                        <div>Total points</div>
                    </div>
                    <% for (let i = 0; i < users.length; ++i) { %>
                    <div class="entry3">
                        <div><%= i + 1 %></div>
                        <div><%= users[i].user_name %></div>
                        <div><%= users[i].totalpoints %></div>
                    </div>
                    <% } %>
                <% } %>
            </div>
        </div>
    </main>

    <%- include(`partials/footer`); %>
</body>

</html>
